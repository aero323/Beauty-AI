# Beauty AI 管理后台产品笔记

## 学习积分与大区排名

管理后台的大区排名应与学员端 APP 使用同一套积分口径。排名不使用复杂模型，不做 100 分归一化，也不引入时间系数或连续性等难解释指标。目标是让培训师能解释清楚：每个 BA 的排名来自哪些可见学习行为。

排名对象是在同一大区、同一统计周期内的 BA。默认使用月榜，后续可以增加周榜。榜单展示“学习积分”，按积分从高到低排序。

积分规则：

- 完成学习任务：每个任务 +20 分。
- 完成练习任务：每个任务 +20 分。
- 完成考试任务：每个任务 +20 分。
- 学完课件：每个课件 +5 分。
- 有效练习：每次 +5 分。有效练习包括 AI 陪练、剧本练习、金句跟读，但必须来自任务要求或练习页的正式完成记录。
- 考试成绩奖励：考试通过后按成绩给奖励分，`考试奖励分 = round(考试分数 / 10)`。例如 95 分加 10 分，82 分加 8 分。未通过不加奖励分；如果考试任务规则允许“提交即完成”，仍可拿考试任务完成分。

公式：

`学习积分 = 已完成任务数 * 20 + 已学课件数 * 5 + 有效练习次数 * 5 + 考试奖励分合计`

简单限制：

- 同一个课件只计一次学完积分。
- 同一个练习任务的有效练习次数最多按任务要求次数计分，例如任务要求每周 3 次，最多按 3 次加练习分。
- 同一场考试按单次最终成绩计算奖励分。
- 后台可以标记任务是否参与排名；临时测试任务、草稿任务不计入积分。

同分排序：

- 完成任务数更多者优先。
- 考试奖励分更高者优先。
- 有效练习次数更多者优先。
- 已学课件数更多者优先。

## 积分结算周期

学习积分采用“平时实时累计，月底冻结归档”的机制。

- 学员完成任务、课件、练习、考试后，积分即时更新。
- 学员端 APP 展示“本月截至当前”的实时积分和实时排名。
- 管理后台展示同一实时榜单，并可查看积分构成。
- 每月最后一天 23:59 后冻结当月榜单，生成月度排名记录。
- 次月 1 日开启新的月榜，学习积分从 0 开始累计。
- 历史月榜需要保留，后台应支持查看、筛选和导出。

后台需要提供的管理能力：

- 查看 BA 的学习积分总分和积分构成。
- 查看大区、门店、个人维度的月榜。
- 标记任务是否参与排名。
- 查看积分来源明细，例如任务完成、课件完成、有效练习、考试奖励。
- 查看历史月榜，用于培训复盘和激励。

## 自动数据统计与学习提示实现口径

管理后台里的“数据波动与学习提示”“高风险 BA”“知识盲区”等能力，不应理解成让 LLM 定期自由分析员工数据。生产实现应拆成“确定性统计、规则判断、可选文案生成”三层。

第一层是数据统计层，由后端基于结构化数据稳定计算：

- 学习任务完成率：来自任务表和学员任务进度表。
- 课件学习进度：来自课件观看、开始、完成记录。
- 考试成绩与错题：来自考试提交记录、题目标签、知识点标签、产品标签。
- 练习次数和时长：来自数字人顾客、场景剧本、金句跟读的正式完成记录。
- 登录活跃：来自登录日志或访问事件。
- 任务来源和类型：来自任务配置中的总部/区域、学习/练习/考试字段。

第二层是规则判断层，用明确规则生成可解释的结构化结论。示例：

- 某知识点错题率 >= 50%，且有效答题数 >= 3，生成“知识点薄弱”提示。
- 本月练习次数 = 0，生成“练习缺失”提示。
- 连续 7 天未登录，生成“活跃风险”提示。
- 最近考试分数 < 60，生成“考试风险”提示。
- 任务完成率 < 50%，且距离截止时间 < 3 天，生成“任务逾期风险”提示。

第三层是可选的 LLM 文案层。LLM 只负责把后端已经算出的结构化事实写成更自然的管理端提示，不负责判断员工是否高风险，也不能自行添加不存在的数据。更保守的首期方案可以完全不用 LLM，直接用模板生成文案。

建议落库结构：

- `ba_learning_daily_summary`：保存 BA 每日学习汇总，例如任务完成率、最近考试分数、已学课件数、练习次数、练习时长、当月积分。
- `ba_knowledge_weakness`：保存 BA 在产品线、产品、知识点维度上的错题数、总题数、错误率。
- `ba_learning_insight`：保存结构化提示，包括提示类型、严重程度、证据 JSON、展示文案、建议动作、生成时间。

建议计算机制：

- 学员完成任务、课件、练习、考试时，实时写入原始行为记录。
- 每日凌晨跑 `daily_ba_learning_summary_job`，聚合员工学习画像、知识薄弱点、风险标记和学习提示。
- 每周/月末跑排行榜、积分归档和周期复盘数据。
- 管理端打开员工详情时读取最近一次聚合结果，不在页面请求时临时跑大查询或 LLM 分析。

结论：后台规则和数据聚合负责“结论”，LLM 最多负责“表达”。这样研发可实现、管理者可解释、问题可追溯。

## Practice 模块与学员端对齐复查

### 结论

- 后台 `BAAvatars`、`BAScripts`、`BAQuotes` 和 `PracticeTaskManage` 的对象结构是清楚的：数字人顾客、场景剧本、金句库、任务组合四层。
- 学员端已经把任务详情按这三类资产拆开了，方向是对的。
- 但从资产定义看，学员端在两个地方还是偏重：一是数字人对练被做成了完整聊天产品，二是剧本演练被写死成固定步数流程。
- 金句库这一块学员端已经开始按产品线和产品组织，但还需要继续收敛成“产品金句练习”，不要只剩单句跟读的消费流形态。

### 后续建议

- 后台继续保持“资产配置”口径，不要把结果页评分维度扩成过多不可解释字段。
- 任务层保持“任务实例 + 资产组合 + 覆盖 / 频次规则”的表达，这样才能和学员端的完成回传统一。
