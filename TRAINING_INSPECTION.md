# 培训巡检 Agent

## 当前交付

后台新增「培训巡检」，提供全国巡检、区域周计划、整改跟踪、日报/周报及文本导出。总部培训师可管理策略、任务和例外；区域角色只查看南区演示范围，区域经理只读。沿用项目的角色模拟器，不构成生产鉴权。

这是可交互的前端演示闭环。规则计算、任务调整、完成回传、整改复查、通知合并和本地持久化均可运行；没有新增后端服务、外部消息渠道或模型依赖。界面明确标记模拟数据与本地运行。

## 验证流程

1. 总部角色打开培训巡检。南区示例人均 160 分钟，总部 100 + 区域 60，容量为示例值 90；问题自动交给总部任务负责人。
2. 打开整改详情，提交措施与承诺日期，再提交复查。不调整任务时，重新巡检仍应保持处理中。
3. 打开区域计划，调整任务耗时、频次、日期、分配对象或启停状态。保存立即重算；规则消失后自动关闭问题，再次命中时重新开启并记录复发。
4. 给重复任务填写复训理由，验证重复安排问题关闭。例外申请需要总部审批，保留原因与到期日，到期重新巡检。
5. 在管理策略中切换「自动提醒（模拟）」，填写变更说明。打开站内通知查看按负责人合并的通知，再次巡检同日不增加重复通知。
6. 切换区域培训主管，确认只有南区数据、总部任务不可编辑、总部负责的问题不能由区域代办。区域经理所有整改与任务操作只读。
7. 任务详情的「本周期完成记录 · 模拟回传」可登记单个学员的完成次数，验证剩余负荷和进度问题重算。

## 数据契约与计算口径

`src/lib/inspectionTypes.ts` 定义任务、逐人分配、完成周期、区域容量、管理策略、问题单、事件与通知契约；`inspectionEngine.ts` 是不依赖 React 和模型的计算与状态转换层。

- 学习、考试、媒体采集、练习共用契约；每次耗时包括附加题，单位为分钟。
- 一次性任务归入截止所在周；跨周任务不会每周重复计量。每日任务按实际有效日计算，每周任务每有效周只计要求次数。日期按 `Asia/Jakarta` 解释，周一到周日为完整周期。
- 完成记录键：一次性为 `once`，每日为 `YYYY-MM-DD`，每周为周一日期。已完成次数只能消耗对应周期的剩余量，不能跨周期抵扣。
- 人均与 P90 基于区域当前有效人员。历史不足四周期或关键字段缺失时标记数据不足；默认 1.5 倍历史中位数只表示疑似异常。
- 容量以逐人已知负荷判断。参考容量 90 / 120 分钟、人数和历史都来自演示数据，不代表客户已经确认的管理标准。
- 任务叠加、重复、覆盖、截止集中、临期进度、待复核压力均生成可追溯的结构化证据。复查通过表示安排或对应规则已改善，不等于学员已完成全部培训。
- 问题以周期、区域、规则和对象组合去重；保存责任、期限、例外、时间线和复发次数。负荷等规则对当前事实重新计算，证据变化留痕。
- 默认下一个工作日响应，周末顺延；当前不接入印尼地区节假日日历。

## 与现有任务页的关系

现有学习、练习、考试及媒体任务通过 `syncInspectionSource` 同步原始任务摘要，页面创建或停用后刷新接入状态。旧页面自身仍沿用原来的 mock 生命周期。

这些摘要缺少逐人分配、结构化频次、责任人与可信耗时，因此在「数据接入」中标记待补齐契约，不伪造分配明细，不混入演示负荷。演示任务保存在独立统一契约中，调整不会静默修改旧页面 mock。接入真实任务后，应让旧任务编辑页与巡检读取同一后端任务版本，替换这层摘要适配。

## 运行与持久化

- 状态保存在浏览器 `localStorage` 的 `salesboost.training-inspection.v1`；无跨设备同步。存储不可用时显示提示并降级为当前会话。
- 页面打开期间每分钟执行巡检；任务和完成记录变化立即触发。默认检查本周、下周、用户选择的周与未关闭问题涉及的周。
- 默认观察模式。模拟自动提醒可手工开启，仅生成本地站内通知；同一责任人同日只保留一条汇总，后续巡检更新内容而不重发。逾期区域问题同时进入总部升级汇总。
- 简报使用统计结果和模板，不调用 LLM。因此模型不可用不会阻断提醒与复查。
- 新工作台目前中文呈现；导航已接入英文和印尼语字典。

## 生产接入

1. 后端补齐统一任务、人员归属与在岗状态、完成明细、负责人、耗时、策略和例外数据；任务编辑使用版本控制，保留历史分配快照。
2. 将规则和状态机部署到受鉴权的后端，区域范围由登录身份决定，禁止信任浏览器的角色与责任人字段。
3. 接入任务、人员与完成事件，5 分钟内增量计算；服务端每日全量校验。浏览器定时器不能承担关页后的后台巡检。
4. 通知落库使用负责人/日期幂等约束，发送与读取分离，接入真实站内消息中心和客户日历。外部渠道另行实施。
5. 使用至少两个完整周的真实数据观察误报，由总部确认基线后开启真实自动提醒；不使用模拟模式开关绕过生产观察期。
6. 可选模型只接收结构化事实与少量证据，生成解释/简报；保持规则结论、权限、责任归属和状态机确定性。

## 验证命令

```sh
npm run lint
npm run build
npm run test:inspection
```

浏览器验证覆盖桌面 1440px、手机 390px、四个视图、策略弹窗、通知去重与已读、整改回应、失败复查、例外审批、任务调整、自动关闭、刷新持久化及区域权限。截图位于 `output/playwright/`。
